/**
 * 샷 입력 한도 — 화면(simReducer)과 서버 검증(sim · simDrill · simMatch 라우트)이 **같은 값**을 본다.
 * 2026-09-26: 서버는 V0 12 · 들림각 1.2 rad 까지 받아, 고친 클라이언트가 누구보다 60 % 센 샷을 칠 수 있었다.
 * 앱은 원격 사이트(capacitor server.url)를 불러오므로 모든 사용자가 같은 한도의 화면을 쓴다.
 *
 * 타격 모델(resolve/stickBall)은 큐 속도를 공 속도로 약 1.25배 한다(캐롬 공 210 g, 큐 520 g, 팁 효율 0.88):
 *   9 m/s → 공 11.3 m/s — 포켓볼 브레이크 수준이라 3쿠션에서는 쓰지 않는 세기였다.
 *   7.5 m/s → 공 9.4 m/s — 대회전(공 5~7 m/s)은 70~85 % 에서 나오고, 100 % 는 그보다 세지만 브레이크에는 못 미친다.
 */
export const V0_MAX = 7.5;
/** 엔진이 받아 주는 상한 — 상한을 낮추기 전(2026-09-12 이전)에 저장된 샷과 공유 링크 재생용. 새 샷 검증에는 쓰지 않는다. */
export const V0_LEGACY_MAX = 9;
/** 큐 들림각 상한(rad) — 60°. 엔진 v2.2 가 공중·착지를 풀어 마세이(30°+)·점프(35°+)가 나온다. */
export const THETA_MAX = (60 * Math.PI) / 180;
/** 서버 검증 여유 — 화면이 한도에 맞춰 보낸 값이 부동소수 반올림으로 살짝 넘어도 받는다. */
export const SHOT_LIMIT_EPS = 1e-6;
