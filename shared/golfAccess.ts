/**
 * 골프 접근 통제의 **공개해도 되는 부분**만 여기 둔다 — 이 파일은 화면 번들에도 실린다.
 *
 * 2026-09-11: 예전엔 시험 참가자 전화번호 목록이 여기 있어서 **오너 개인 휴대폰 번호가 공개 JS 번들에**
 * 실렸다. 게다가 가입 때 번호를 확인하지 않아서 그 번호로 새로 가입하면 골프 문이 열렸다. 판단은 서버
 * (server/lib/golfAccess.ts)가 회원·프로필 id 로 하고, 화면은 GET /api/hiq/me 의 golfAccess 한 값만 본다.
 *
 * 오너 방침(2026-09-09): 골프와 당구는 아예 다른 플랫폼으로 본다. 시험 단계라 허용된 사람만 들어간다.
 * 전면 공개할 땐 GOLF_PUBLIC 을 true 로 한 번만 바꾼다.
 */

/** 전면 공개 스위치. true 면 허용 목록과 무관하게 모두 골프를 쓸 수 있다. */
export const GOLF_PUBLIC = false;
